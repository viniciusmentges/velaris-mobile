// Stub — RevenueCat pendente (Etapa 4).
// Substitua pela integração real quando react-native-purchases estiver instalado.

export async function configurePurchases(_userId) {
    // no-op: RevenueCat não integrado ainda.
}
